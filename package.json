{
  "name": "parse-app",
  "version": "0.1.0",
  "description": "This is a basic parse app.",
  "keywords": [
    "parse"
  ],
  "engines": {
    "node": ">=4.6"
  },
  "dependencies": {
    "express": "^4.16.2",
    "mongodb-runner": "^3.2.1",
    "parse-server": "^2.6.5"
  },
  "scripts": {
    "start": "mongodb-runner && node index.js"
  }
}
